import { WarnClassPipe } from './warn-class-pipe';

describe('WarnClassPipe', () => {
  it('create an instance', () => {
    const pipe = new WarnClassPipe();
    expect(pipe).toBeTruthy();
  });
});
